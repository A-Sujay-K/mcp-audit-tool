# MCP Audit Sandbox — restricted Debian slim container
RUN pip install --no-cache-dir httpx
RUN useradd -m -s /bin/bash sandboxuser
WORKDIR /app
RUN chown sandboxuser:sandboxuser /app
USER sandboxuser
ENTRYPOINT ["python"]
